{
  "name": "task-loop",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --compile --outfile $HOME/.local/bin/task-loop index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.0.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
