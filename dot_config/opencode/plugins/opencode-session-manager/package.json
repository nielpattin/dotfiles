{
  "name": "opencode-session-manager",
  "version": "1.0.0",
  "description": "Standalone session management tools for OpenCode",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm && bun x tsc --emitDeclarationOnly",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.218"
  },
  "devDependencies": {
    "bun-types": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
